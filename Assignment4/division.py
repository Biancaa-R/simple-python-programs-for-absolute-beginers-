#Perform the division operation and find the quotient and remainder values. (Withoutusing /, // % operators)

divident=int(input("Enter the divident"))
divisor=int(input("Enter the divisor"))
quotient=0
rem=0

while num> divisor:
    divident=divident-divisor
